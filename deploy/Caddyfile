{$CADDY_SITE_ADDRESS} {
    encode gzip zstd

    @api path /api/* /health /media/*
    handle @api {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
